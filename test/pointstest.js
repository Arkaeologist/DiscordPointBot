var expect = require('chai').expect;
var givePoint = require('../points.js').givePoint;
var listPoint = require('../points.js').listPoint;

describe("#givePoint()", function() {
  it.skip("adds points to a user", function() {
    var pointsArray = ['!givePoint', 5, 7];
    var mentionsArray = ['109134866209095680', '140740133648465920'];
    pointBot.givePoint(msg.server, msg.channel, msg.mentions, parsedMessage);
  });
});

describe("#listPoint()", function() {
  it.skip("lists points of mentioned users", function() {

  });
});
